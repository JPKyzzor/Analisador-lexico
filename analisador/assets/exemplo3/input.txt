void main{
    retFunc : integer;
    msgFinal : string;
    valorA : integer;
    valorB : integer;

    integer somaInt(integer a; integer b){
        parcial : integer;

        inicio
        cin >> parcial;
        do{
            parcial = parcial + 1.2;
        } while(parcial < 5);

        cout << `Soma é maior que 5`;
        fim
        return(parcial)
    }

    void boasVinda{
        inicio
            cout << `Bem vindo!`;
            cout << `Menu:`;
            cout << `1 - Opcao 1`;
            cout << `2 - Opcao 2`;
        fim
        return()
    }

    inicio
        valorA = 5;
        valorB = 3;
        callfuncao boasVinda;
        retFunc = callfuncao somaInt(valorA, valorB);
        cout << `Resultado final: ` << retFunc;
    fim
}